{
  "name": "md-links-amapola",
  "version": "0.1.0",
  "description": "modulo que permite validar links de un archivo",
  "engines": {
    "node": ">=14.x"
  },
  "devDependencies": {
    "jest": "^27.5.1"
  },
  "scripts": {
    "test": "jest --coverage --verbose"
  }
}
